# Assignment 06: K-Means and DBSCAN Clustering

## Objective
Perform clustering on synthetic 2D data using:
- **K-Means** (centroid-based clustering)
- **DBSCAN** (density-based clustering)

Compare both methods using silhouette score and inspect cluster behavior.
